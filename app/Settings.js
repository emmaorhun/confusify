import React from 'react';
import {
  AppProvider,
  Page,
  Card,
  Button,
  ResourceList,
  TextStyle,
  Avatar,
} from '@shopify/polaris';

export default function Settings() {
  return (
    <AppProvider>
      <Page title='Settings'>
      </Page>
    </AppProvider>
  )
}
